import { makeStyles } from "@mui/styles";

const ButtonCoStyle = makeStyles(()=> ({
    root:{
        color:'white',
        backgroundColor: "--primary-main"
    }

}));
export default ButtonCoStyle;